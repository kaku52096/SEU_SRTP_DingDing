/*
using UnityEngine;

public class trigerbutton : MonoBehaviour
{
    // Start is called before the first frame update
    private void OnTriggerEnter(Collider other)
    {
        Debug.Log("检测到碰撞!!!!!!!!");
        ElevatorControllBtn btn =GetComponent<ElevatorControllBtn>();
        if (btn != null)
        {
            //上述都还是碰撞检测，鼠标并没有按下


            //  Debug.Log("检测到碰撞");
            //Elevator.My_TaskQueen.AddTask(btn.press);
            btn.press();
            
        }
    }
}*/
